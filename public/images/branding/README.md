# Company branding

The company logo is stored here as:

```text
NRZ.webp
```

The logo is used by gate-pass printouts. To use another public image path, set `BRAND_LOGO` in `.env`, for example:

```env
BRAND_LOGO=images/branding/another-logo.png
```
